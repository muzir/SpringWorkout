<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:tx="http://www.springframework.org/schema/tx" xmlns:aop="http://www.springframework.org/schema/aop"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc
http://www.springframework.org/schema/mvc/spring-mvc-4.1.xsd
http://www.springframework.org/schema/beans
http://www.springframework.org/schema/beans/spring-beans-4.1.xsd
http://www.springframework.org/schema/tx
http://www.springframework.org/schema/tx/spring-tx-4.1.xsd
http://www.springframework.org/schema/context
http://www.springframework.org/schema/context/spring-context-4.1.xsd
http://www.springframework.org/schema/aop
http://www.springframework.org/schema/aop/spring-aop.xsd">

	<bean id="personA" scope="prototype" class="com.springWorkout.domain.Person">
		<property name="name" value="PersonA"></property>
	</bean>
	<bean id="personB" scope="singleton" class="com.springWorkout.domain.Person">
		<property name="name" value="PersonB"></property>
	</bean>

	<context:component-scan base-package="com.springWorkout.dao"></context:component-scan>
	<context:component-scan base-package="com.springWorkout.service"></context:component-scan>
	<context:component-scan base-package="com.springWorkout.controller"></context:component-scan>

	<bean id="dataSource" class="org.apache.commons.dbcp.BasicDataSource">
		<property name="driverClassName" value="${jdbc.driverClassName}" />
		<property name="url" value="${jdbc.url}" />
		<property name="username" value="${jdbc.userName}" />
		<property name="password" value="${jdbc.password}" />
		<property name="initialSize" value="5" />
		<property name="maxActive" value="10" />
	</bean>
	<context:property-placeholder location="classpath:/database.properties" />

	<!-- the transactional advice (what 'happens'; see the <aop:advisor/> bean 
		below) -->
	<tx:advice id="txAdvice">
		<tx:attributes>
			<tx:method name="get*" read-only="true" />
			<tx:method name="*" read-only="false" />
		</tx:attributes>
	</tx:advice>

	<aop:config>
		<aop:pointcut id="PersonServiceMethods"
			expression="execution(* com.springWorkout.service.PersonService.*(..))" />
		<aop:advisor advice-ref="txAdvice" pointcut-ref="PersonServiceMethods"></aop:advisor>
	</aop:config>

	<bean id="transactionManager"
		class="org.springframework.orm.hibernate4.HibernateTransactionManager">
		<property name="sessionFactory" ref="sessionFactory" />
	</bean>

	<tx:annotation-driven />

	<!-- Settings -->
	<bean id="sessionFactory"
		class="org.springframework.orm.hibernate4.LocalSessionFactoryBean">
		<property name="dataSource" ref="dataSource" />
		<property name="hibernateProperties">
			<props>
				<prop key="hibernate.dialect">org.hibernate.dialect.MySQL5Dialect</prop>
				<!-- The list of possible options for hibernate.hbm2ddl.auto property 
					are, -->
				<!-- validate: validate the schema, makes no changes to the database. -->
				<!-- update: update the schema. -->
				<!-- create: creates the schema, destroying previous data. -->
				<!-- create-drop: drop the schema at the end of the session. -->
				<prop key="hibernate.hbm2ddl.auto">validate</prop>
				<prop key="show_sql">false</prop>
				<prop key="hibernate.validator.autoregister_listeners">true</prop>
			</props>
		</property>
		<property name="packagesToScan">
			<list>
				<value>com.springWorkout.domain</value>
			</list>
		</property>
	</bean>
</beans>